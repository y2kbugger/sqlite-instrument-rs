[project]
name = "sqlite-instrument-test"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.1",
    "tuplesaver",
]

[tool.uv.sources]
tuplesaver = { git = "https://github.com/y2kbugger/tuplesaver.git" }
